node_modules/
dist/
*.local
.DS_Store
*.tsbuildinfo

# build_fixtures.py가 WorkerId 해시에 쓰는 salt. 저장소에 넣지 않는다 (scripts/build_fixtures.py 참고).
scripts/.fixture_salt
# 변환할 원본의 목록. 내부 경로와 파일 이름이 들어 있다. 형식은 fixture_sources.example.json 참고.
scripts/fixture_sources.json
__pycache__/

# mock API 서버의 SQLite 파일. 지우면 다음에 시작할 때 data/에서 다시 만들어진다.
var/

feedback/